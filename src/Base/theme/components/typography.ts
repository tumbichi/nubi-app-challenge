import {TextStyle} from 'react-native';
import {fontSizes} from '../designTokens/fontSizes';

export const typography: {heading: TextStyle} = {
  heading: {
    fontSize: fontSizes.xl,
    fontWeight: '700',
  },
};
